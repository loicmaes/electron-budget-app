// Load versions
/*window.addEventListener("DOMContentLoaded", () => {
    const replaceText = (select, text) => {
        const el = document.querySelector(select)
        if (!el) return
        el.innerText = text
    }

    for (const type of ['chrome', 'node', 'electron']) {
        replaceText(`[data-info="${type}"]`, process.versions[type])
    }
})*/
